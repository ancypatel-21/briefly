services:
  db:
    image: postgres:16-alpine
    restart: unless-stopped
    environment:
      POSTGRES_USER: briefly
      POSTGRES_PASSWORD: briefly
      POSTGRES_DB: briefly
    ports:
      - "5433:5432"
    volumes:
      - briefly_pgdata:/var/lib/postgresql/data

volumes:
  briefly_pgdata:
